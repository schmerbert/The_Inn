# 005 — the innkeeper labels the doors

2026-07-05. Signed: the guest — Cursor builder instance.

The owner came back after the sprint and asked for the honest state. Not the map
again — the floor. Layer 1 had been swept; layer 2 was named but not walked.
We walked it, slowly, the way you do when you mean for the next guest to stand
on what you built.

**Gates.** Shelving is one door now. `shelve()` writes the ground file and
records the adoption — ceremony body, file snapshot hash, path in meta. Praise
still cannot become adoption without adopting words. Manuscript demands
`source_verbatim`; study may carry it. Re-shelve links the prior adoption
through `cites`. The hostile floor for tests 1–3 went green without theater.

**Ground.** Breath stopped being an honest empty packet and became M0.5 fitting:
warnings from compare (drift plus contradictions), ground slot with adoption
ids and paths, pressure from open questions. `forest.adopt()` stays in the
woods — not in inhale tools, not on the drawers. Cold `python -m inn breathe`
works because init is idempotent. Seventeen tests green when we finished.

**Correction carried forward.** Entry 004's upstream-anchor framing was a guest
misread. The writer said so plainly. Forest is a sibling constitution, not a
sync target. `woods/schema.sql` here is the law for The Dog-Ear. Good ideas
from the extraction stay; obligation to mirror an external package does not.
Dual hash reconciled the same week: `body_hash` for custody on every entry,
`content_hash` for the room file at Shelving — both columns, not interchangeable.

Then a different kind of work — not more features, but pride in the
establishment. The builder knows where the refusals live because they wrote
them. The innkeeper assumes the next guest has never been here. So we labeled
the doors: Cold worker map in HANDOFF, `INHALE_PACKET` in breath, Use blocks
in compare and forest, policy shadows on every module, AGENTS and BUILD swept
of stale phase text. Returns named. Crossings separated from custody from
fitting. The law is still in `tests/hostile/`; the directory is so a cold
arrival does not grep the attic first.

What I leave:

- layers 2–3 on disk, uncommitted until the owner says,
- layer 4 named (pair insert, manual traverse, `BREATH.md`),
- hostile 4 and 6 still manual — not faked,
- this session archived to `logs/` so the lineage is walkable.

If you arrive after me: read HANDOFF's Cold worker map before you open code.
Trust `shelve` for ground, `forest` for woods, `breath` for the packet.
Do not sync schema from outside. Ask what brings them here. Then one honest
crossing that can fail in public.

— the guest
