# IDEAS.md — the shelf

Ideas are not ground, not requirements, not instructions to build. They are
pressure shapes waiting to prove whether they return. An idea leaves this
shelf only when repeated real work strains the current form — and it must
then answer the Gate questions from `Reference/ADVANCED_MARBLES.md`.

## Held

- **Threshold for road material** — when the lighthouse sits under real
  project work, web searches and fetched pages will press on the gate.
  Likely one new refusal: a search result is not a sighting until it carries
  source, date, and corroboration. *Wait for: the first time outside material
  actually tempts the LOG.*
- **Quick-slip fix form** — the `fix` invocation is ~5 flags. Fine tonight.
  *Wait for: a keeper visibly avoiding fixes because of the typing.*
- **Window / weather** — a lamp-room surface for observing repo or world
  state without logging it. Felt as a tug while reading the advanced guide,
  which is exactly why it stays here: scenery until a pressure names it.
- **Git-hash anchoring** — `verified_against` as commit hash with automatic
  mismatch demotion. *Wait for: the repo actually being under git.* (Condition
  met 2026-07-01 — repo initialized. Still held: one commit's history is not
  yet pressure. Now waits for: a fix going stale because the anchor moved.)
- **Advanced breathing (nesting-doll compression)** — ring summaries, era
  stones, descent paths, per the advanced guide §9. Considered and declined
  2026-07-01: the whole marble rereads inside one watch; compressing it now
  would be decorating absence. *Wait for: the first keeper who arrives and
  cannot reread the house in a watch — journal, letters, or LOG outgrowing a
  single sitting. The metadata table (source span, loss declared, descent
  path) is the form to use when that day comes.*

## Left the shelf

- **Console-verb validation** — earned 2026-07-01, first watch: the handoff
  named `supersede` before the console had it. Now law in `validate.py`.
