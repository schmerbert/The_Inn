""".cabin — a local-first context-and-memory harness.

Serves the worker's coherence, not the task. Local-only: embeddings on-device,
storage in SQLite. Read CLAUDE.md for the reading path, then CABIN.md for the
two LAWS that every line here answers to.
"""
