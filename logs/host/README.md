# Host turn envelopes + stay transcripts (layer 5)

- `*-turn.json` — fit/model timings + `pair_root_id` (no prose)
- `*-stay.md` — **full** writer/guest transcript for one CLI stay

Both `*.json` and `*-stay.md` are gitignored (local). See `HOST.md`.
